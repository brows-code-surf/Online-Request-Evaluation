import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from '../utils/authContext';

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
  fallback: [
    '-apple-system',
    'BlinkMacSystemFont',
    'Segoe UI',
    'Roboto',
    'Oxygen',
    'Ubuntu',
    'Cantarell',
    'Fira Sans',
    'Droid Sans',
    'Helvetica Neue',
    'sans-serif'
  ]
});

export const metadata = {
  title: "Online Requests Evaluation System",
  description: "Generated by create next app",
  icons:{
    icon: 'SANTEH LOGO/SFC.png',
  }
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
